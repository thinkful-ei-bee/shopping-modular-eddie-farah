'use strict';
/* global shoppingList */
/* global $ */
/* global store */
/* global Item */

// eslint-disable-next-line no-unused-vars

$(document).ready(function() {
  shoppingList.bindEventListeners();
  shoppingList.render();
  
});
